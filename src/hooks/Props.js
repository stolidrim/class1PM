import React from 'react';

const Props = (props) => {
  return <>
        <center><h1>I love {props.language} Program. It is used for {props.technolog}.</h1></center>
  </>;
};

export default Props;
